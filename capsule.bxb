capsule {
  id (example.bank)
  version (2.0.1) // Layout updates
  format(3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.core) { 
      as (core) 
    }
    import (viv.common) {
      as (common) 
      version (3.24.0)
    }
    import (viv.money) { 
      as (money) 
      version (2.16.17)
    }
    import (viv.time) { 
      as (time) 
      version (2.60.10)
    }
  }
}
