capsule {
  id (example.bank)
  version (2.0.4)
  format(3)
  runtime-version (2)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.core) { 
      as (core) 
    }
    import (viv.money) { 
      as (money) 
      version (2.22.43)
    }
    import (viv.time) { 
      as (time) 
      version (3.1.188)
    }
  }
  store-countries {
    all
  }
  store-sections {
    section (BusinessAndFinance)
  }
}
