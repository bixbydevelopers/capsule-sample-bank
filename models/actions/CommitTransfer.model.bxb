action (CommitTransfer) {
  type (Commit)

  confirm {
    by (common.Confirmation)
  }

  collect {
    input (transfer) {
      type (Transfer)
      min (Required)
    }
  }
  
  output (TransferReceipt)
}