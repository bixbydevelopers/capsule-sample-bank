input-view {
  match : FromAccount(this)
  message {
    template-macro (WhichFrom)
  }
  render {
    selection-of (this) {
      where-each (account) {
        //Uses the default layout for each 'account' in 'this'
        
      }
    }
  }
}