input-view {
  match : ToAccount(this)
  message {
    template-macro (WhichTo)
  }
  render {
    selection-of (this) {
      where-each (account) {
        //Uses the default layout for each 'account' in 'this'
      }
    }
  }
}